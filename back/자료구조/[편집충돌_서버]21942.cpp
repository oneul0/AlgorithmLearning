#include <iostream>
#include <map>
#include <string>
#include <sstream>


using namespace std;

string deadline;

int N, DDD, hh, mm, F; //첫번째 줄 입력



int main(){
    //ios_base::sync_with_stdio(0);
    cin >> N >> deadline >> F;
    stringstream ss;
    ss.str(deadline);
    
    return 0;
}