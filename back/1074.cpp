#include <bits/stdc++.h>
#include <cmath>
using namespace std;


int n,r,c, mapSize, a,b;

int main(){
    ios_base::sync_with_stdio(0); cin.tie(0);
    cin >> n >> r >> c;
    mapSize = pow(2,n);
    
    return 0;
}